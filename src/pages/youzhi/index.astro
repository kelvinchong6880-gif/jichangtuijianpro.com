---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getBrands } from '../../utils/data';

const allBrands = await getBrands();
const refundDisclosedCount = allBrands.filter((b: any) => b.data.refund_policy).length;
const deviceLimitDisclosedCount = allBrands.filter((b: any) => b.data.device_limit).length;

const faqs = [
  {
    q: '怎么判断一个机场是不是优质机场？',
    a: '不要只看宣传语，重点看官网有没有讲清楚退款政策、设备限制这类具体条款，以及是否愿意提供真实的测速/解锁证据。信息越透明，通常越值得信赖。'
  },
  {
    q: 'IPLC专线和IEPL专线哪个更好？',
    a: '两者都属于专线，理论上比普通中转线路更稳定，但具体表现受节点部署、当时网络环境影响很大，官方标注的线路类型不能单独证明实际速率，建议自己实测。'
  },
  {
    q: '优质机场一定贵吗？',
    a: '不一定。价格和实际质量之间没有必然的正比关系，本站也没有独立测速数据能证明"贵=好"，具体建议先用短周期套餐验证再决定。'
  },
  {
    q: '机场晚高峰会降速吗？',
    a: '这是很多机场的通病，具体是否降速、降多少因品牌和节点而异，本站目前没有对收录品牌逐一做晚高峰测速，购买前建议直接问客服或先用短期套餐自己测试。'
  }
];

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map(f => ({
    '@type': 'Question',
    name: f.q,
    acceptedAnswer: { '@type': 'Answer', text: f.a }
  }))
};
---

<BaseLayout title="优质机场怎么选：证据与限制核对" description="核对机场服务质量所需的测试证据、套餐限制和资料来源。当前收录品牌尚无本站独立实测。" structuredData={faqSchema}>
  
  <div class="max-w-4xl mx-auto space-y-8">
    <div class="border-b border-gray-200 pb-5">
      <h1 class="text-3xl font-extrabold text-gray-900">优质机场怎么选</h1>
      <p class="mt-3 text-xl text-gray-500">
        价格高、线路名称或官方宣传不能单独证明服务质量。当前收录品牌均未由本站独立实测，因此这里暂不发布质量排名。选择时请核对您所在地区和使用时段的晚高峰测试、流量倍率、设备限制与退款条件。
      </p>
    </div>

    <div class="prose prose-sm max-w-none text-gray-700 space-y-4">
      <h2 class="text-xl font-bold text-gray-900">优质机场推荐：与其看宣传语，不如看信息透明度</h2>
      <p>
        "全 IPLC 专线""原生 IP 解锁 Netflix"这类宣传语，几乎每家机场官网都会写，单靠这些话判断不出谁更优质。本站认为一个更实际的信号是：<strong>这家机场愿不愿意把退款政策、设备限制这类具体条款讲清楚</strong>——愿意公开细则的，通常对自己的服务更有底气。
      </p>
      <p>
        据实说明：本站当前收录的 {allBrands.length} 家品牌中，官网公开资料<strong>明确写出退款政策的有 {refundDisclosedCount} 家、明确写出设备台数限制的有 {deviceLimitDisclosedCount} 家</strong>。这不代表其余品牌不支持退款或不限制设备，而是官方公开资料里没有讲清楚——这本身就是购买前需要向客服问清楚的地方，也是本站没法据此做质量排名的原因之一。
      </p>
      <p>
        建议在决定购买前，直接找官方客服问三件事：晚高峰速率大概是什么水平、退款政策具体条款、同时能登录几台设备，把回复截图保存，比看官网的宣传文案更可靠。
      </p>
    </div>

    <div class="space-y-8">
      {allBrands.map((brand: any) => (
        <div class="bg-white border border-gray-200 rounded-lg shadow-sm overflow-hidden">
          <div class="px-6 py-4 border-b border-gray-200 bg-gray-50 flex justify-between items-center">
            <h2 class="text-xl font-bold text-gray-900">
              {brand.data.name}
            </h2>
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-100 text-blue-800">
              证据状态: {brand.data.evidence_status}
            </span>
          </div>
          <div class="p-6">
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
              <div>
                <h4 class="text-sm font-semibold text-gray-900 uppercase tracking-wider mb-2">基础信息</h4>
                <ul class="text-sm text-gray-600 space-y-2">
                  <li>月付记录: {brand.data.price_monthly ? `${brand.data.currency} ${brand.data.price_monthly}` : '资料未提供'}</li>
                  <li>年付记录: {brand.data.price_yearly ? `${brand.data.currency} ${brand.data.price_yearly}` : '资料未提供'}</li>
                  <li>设备限制: {brand.data.device_limit ? `${brand.data.device_limit} 台` : '未说明'}</li>
                  <li>退款政策: {brand.data.refund_policy || '未说明'}</li>
                </ul>
              </div>
              <div>
                <h4 class="text-sm font-semibold text-gray-900 uppercase tracking-wider mb-2">证据范围</h4>
                <div class="bg-gray-100 rounded p-4 text-sm text-gray-600 border border-gray-200">
                  <p>{brand.data.evidence_status}。资料核验日期：{brand.data.verification_date}。</p>
                  <p class="mt-1">暂无可复核的本站晚高峰测速和解锁记录，不据此判断稳定性。</p>
                </div>
              </div>
            </div>
            <div class="mt-4 pt-4 border-t border-gray-200">
              <a href={`/brands/${brand.id}/`} class="text-blue-600 hover:text-blue-500 text-sm font-medium">查看品牌资料与套餐 &rarr;</a>
            </div>
          </div>
        </div>
      ))}
    </div>
    
    <div class="border-t border-gray-200 pt-8">
      <h2 class="text-2xl font-bold text-gray-900 mb-4">常见问题</h2>
      <div class="space-y-4">
        {faqs.map(f => (
          <div class="bg-white border border-gray-200 rounded-lg p-4">
            <div class="font-semibold text-gray-900">{f.q}</div>
            <div class="mt-1 text-sm text-gray-600">{f.a}</div>
          </div>
        ))}
      </div>
    </div>

  </div>

</BaseLayout>
